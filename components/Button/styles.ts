import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    btn: {
		borderWidth: 1,
		borderColor: '#000',
		height: 45,
		width: '100%',
		marginTop: 40,
		justifyContent: 'center',
		alignItems: 'center',
    },
    title: {
		color: '#000',
		fontSize: 16,
		fontWeight: 'bold',
		marginVertical: 10,
	},
});